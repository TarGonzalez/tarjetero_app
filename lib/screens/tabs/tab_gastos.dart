import 'package:flutter/material.dart';

class TabGastos extends StatelessWidget {
  const TabGastos({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      mainAxisAlignment: MainAxisAlignment.center,
      children: <Widget>[
        Text('gastos'),
      ],
    );
  }
}
